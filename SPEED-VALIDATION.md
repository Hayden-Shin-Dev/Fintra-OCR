> 과거 버전의 검증 기록입니다. 현재 기본 엔진과 검증 결과는 [COMPACT-VALIDATION.md](COMPACT-VALIDATION.md)를 확인하세요.

# 속도 개선과 외부 양식 검증 — 2026-09-08

이번 변경은 전체 FintraOCR의 일반화 완료 선언이 아니다. 빠른 1차 추출과 명시적인 모델 추가 분석을 나눴고, 보수적인 근거 검증은 유지했다. JSON 계약은 v2.0이며 전체 적용 필드는 SCHEMA.md와 API /api/schemas에 남는다.

## 변경

- OCR과 필드 매핑은 계속 분리한다. 빠른 경로는 도메인 라벨 후보 + 상대 배치 + 자료형 + 근거 검증을 쓰며 Ollama를 호출하지 않는다. 낯선 의미 표현은 놓칠 수 있다. 회사명·정답·문서 ID·고정 좌표 규칙은 추가하지 않았다.
- 동일 언어/장치/프로필에서는 별도 OCR 프로세스의 모델을 재사용한다. 업로드마다 이미지 인식은 다시 실행한다. 취소·실패·설정 변경 시 종료하며 모델 추가 분석 전에도 종료해 GPU 메모리를 돌려준다. 서버의 stdin 연결이 닫히면 OCR 프로세스도 종료한다.
- 명시적인 Unit 열에서는 사전에 없던 단위도 원문 근거를 보존한다. PC→pcs, Cft→ft3, drum→drum을 검증했다. 숫자만 있는 셀은 단위로 채우지 않는다.
- 모르는 표 헤더도 열 경계로 보존한다. Size/Material/Colour가 품명/수량에 합쳐지는 오류를 막는다. 여러 줄 Unit Value, Sub Total을 연결하고 품목 금액과 문서 총액을 구분한다.
- 날짜는 가능한 해석이 하나일 때만 숫자 날짜를 표준화한다. 27/9/2025는 해석 가능하지만 9/10/2025는 설정 없이는 null이다. 일반적인 DATE 라벨을 무조건 발행일로 가정하지 않는다.
- 원산지/목적국 검증을 완화하지 않았다. The Philippines, Scotland는 이번 화면에서 null/review와 원문 후보를 함께 표시한다. 사용자가 후보와 위치를 검토할 수 있으며 사람의 수정값 저장 기능은 이번 변경에 추가하지 않았다.
- 큰 모델 요청은 모든 토큰/문자열/신뢰도/bbox를 보존한 배열 형식으로 압축한다. 토큰을 잘라 문맥 예산을 맞추지 않는다. 압축 후에도 한도를 넘는 문서는 여전히 명시적으로 실패한다.

## 실측 지연 시간

RTX 4050 Laptop 6GB, GPU PP-OCRv6 Medium, Windows. 초 단위. 아래 전체는 서버 작업 생성부터 완료까지이며 브라우저 파일 업로드 전송 시간은 제외한다. 별도 언급 없으면 실제 이미지 OCR을 다시 실행했다.

| 실행 | 모델 초기화 | OCR | 매핑 | 전체 |
|---|---:|---:|---:|---:|
| 사용자가 제시한 기존 송장 저장 작업 | 9.875 | 2.296 | 269.203 | 283.50 |
| 초기 속도 수정, 프로세스 재사용 전 같은 이미지 업로드 | 18.1 | 4.4 | 0.5 | 26초 |
| 재사용 경로 첫 실행 | 8.234 | 2.797 | 0.594 | 12.16 |
| 같은 이미지 재업로드, OCR 모델 재사용 | 0 | 1.937 | 0.375 | 2.59 |
| 최종 UI 실제 업로드 | 0 | 약 2.8 | 약 0.5 | 화면 4초 |
| 포장명세서, 모델 분석 후 OCR 재초기화 | 14.375 | 2.953 | 0.343 | 18.11 |
| 선하증권, 재사용·반복 업로드 | 0 | 3.953 | 0.422 | 4.65 |
| 외부 48행 송장 첫 OCR | 0 | 7.828 | 1.297 | 9.85 |
| 외부 송장 Ollama 추가 분석, 저장 OCR 사용 | 해당 없음 | 재실행 안 함 | 127.171 | 매핑 약 127초 |

초기화 편차, 문서 크기, 언어 전환, 다른 GPU 작업, 큐 대기 때문에 항상 20초 이내라고 보장하지 않는다. 실제로 첫 빠른 실행은 26초였다. 다른 문서와 동시에 제출한 B/L은 대기 포함 22.53초였으며 위 표의 4.65초는 별도 반복 업로드다. 의미 모델 분석 20초 목표는 미달이다.

## 기존 고정 평가: 모델 보조 경로 → 빠른 경로

동일한 기존 정답을 사용했다. OCR 캐시에서 재매핑한 성적이며 OCR 포함 실행시간 평가와 구분한다. 이전 모델 보조 성적은 기존 요청/응답 재생 기반이다. 빠른 경로는 LLM 응답 재생 없이 실행했다. 각 실제 유형 1장만 있어 모집단 성능으로 일반화할 수 없다.

| 구분 | 이전 정밀도/재현율 | 현재 빠른 정밀도/재현율 |
|---|---|---|
| Sample 상업송장 1장 | 100% / 95.83% | 100% / 95.83% |
| Sample 포장명세서 1장 | 100% / 100% | 100% / 95.24% |
| Sample 선하증권 1장 | 100% / 100% | 100% / 100% |
| 기존 합성 6장 | 100% / 100% | 100% / 100% |
| 별도 생성 합성 3장 | 100% / 100% | 100% / 100% |

포장명세서는 복합 참조 표현의 송장 번호를 빠른 경로가 놓치는 손실이 있다. 송장 기존 gold의 Seller와 현재 Exporter 의미 차이는 정답을 바꾸지 않고 그대로 채점했다. 최신 매핑 함수 시간은 약 0.005–0.083초였고, 실제 작업의 Python 시작/직렬화 포함 매핑 시간은 위 표와 다르다. 정답 없는 신규 필드는 정확도에 합산하지 않는다.

## 외부 문서: 첫 실패와 개발 후 회귀를 분리

출처: [ICCC 공개 국제 전시회 가이드](https://www.iccc.or.id/wp-content/uploads/2020/08/Guide-to-Attending-and-Benefiting-From-International-Trade-Shows-February-2019.pdf), PDF 42페이지/인쇄 38페이지. 공개 작성 예시이며 실제 고객 거래 문서는 아니다. 전체 페이지를 144dpi 이미지로 렌더링했고 문서 영역 크롭이나 정답 좌표를 사용하지 않았다. gold는 첫 OCR 전에 확정하고 SHA256을 data/external/iccc.manifest.json에 기록했다. 제공된 Sample.zip 밖의 문서다.

48개 행 × 8개 적용 품목 필드 + 38개 문서 필드 = 422개 평가 위치, non-null 정답 245개. 별칭 발행일도 기존 방식대로 포함했다. null을 맞힌 것은 정밀도/재현율의 정답 수로 세지 않았다.

| 단계 | 정밀도 | 재현율 | 품목 수 |
|---|---:|---:|---:|
| 처음 본 문서의 수정 전 빠른 결과 | 50.00% | 0.41% | 0/48 |
| 잘못된 문서 합계만 보수적으로 막은 중간 결과 | 100% | 0.41% | 0/48 |
| 일반 표 경계/여러 줄 헤더 개선 후 최종 회귀 | 97.92% | 95.92% | 48/48 |

최종은 추출 240개 중 235개 정확, 정답 245개 중 235개 복구다. 100% 정밀도만으로 개선을 주장하지 않는다. 외부 문서를 분석하고 수정했으므로 최종 성적을 새로운 미사용 양식 성능이라고 부를 수 없다. 중간 버전에서 실제 Ollama도 127초 실행했지만 당시 빠른 경로와 같은 97.92%/95.92%였다.

남은 오류: OCR 글자 누락과 품명/코드 병합, 목적국 주소 해석, 발행일로 단정하지 않은 일반 DATE 라벨. OCR에 없는 글자를 정답에서 가져와 복원하지 않았다. Invoice 스키마에는 이 문서의 전체 상품수량/순중량/총중량과 Size/Material/Colour를 표현할 별도 필드가 아직 없으며 정확도 분모 밖인 스키마 확장 과제로 명시한다. 원본 OCR에는 보존된다.

현재 자료로 새 양식 전체 또는 세 유형의 독립 외부 문서 각각 90%를 입증하지 못한다. 추가 독립 문서와 사전 고정 정답, 특히 외부 Packing List/B/L 평가가 필요하다. 이 조건을 충족했다고 선언하지 않는다.

## 실행 및 결과 확인

현재 수정 코드: C:/Users/shinm/FintraOCR. 개선 서버: http://127.0.0.1:8766 . 기존 8765 서버는 중지하지 않았다. Start-FintraOCR.cmd도 이제 8766을 연다. FINTRA_PORT 환경변수로 포트를 바꿀 수 있다.

실제로 실행한 명령 (프로젝트 폴더, 아래 Python의 절대 경로 사용):

```powershell
& C:/Users/shinm/.cache/fintraocr-venv/Scripts/python.exe -m uvicorn fintraocr.web:app --host 127.0.0.1 --port 8766
& C:/Users/shinm/.cache/fintraocr-venv/Scripts/python.exe -m pytest -q
& C:/Users/shinm/.cache/fintraocr-venv/Scripts/python.exe -m scripts.evaluate_fast
& C:/Users/shinm/.cache/fintraocr-venv/Scripts/python.exe -m scripts.evaluate_external
```

96개 자동 테스트 통과. 실제 UI 업로드, 완료 상태/시간 표시, 모든 적용 필드, 품목 단위와 token ID/문자 구간, 원문 후보 표시, 기존 OCR 재매핑을 확인했다. 모델 요청 실패도 failed 상태로 종료됐다. 상세 수치/JSON은 outputs/speed-study, 최초 외부 OCR은 해당 external-id.txt가 가리키는 data/ui 작업에 있다. 이전 BENCHMARK-V2.md는 과거 버전 결과로 유지한다.
